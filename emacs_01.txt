Time-stamp: <2026-09-09 20:26:13 tito>

~/.emacs
( find-file "~/BV/Emacs/emacs_01.txt" )
( add-hook 'before-save-hook 'time-stamp )

The IceBreaker.
I'm telling you I'm the shit 👍
C-x 8 <Enter> 1f44d <Enter.
ts => tree-sitter

TEXT-WIDTH
C-x f => (set-fill-column) 50
C-x f => (set-fill-column_ 50
C-x h => (mark-whole-buffer)
C-x h => (mark-whole-buffer_
M-x (fill-region)
M-x (fill-region)

################################################
$ cat stm32.txt | fold -w 80 -s > stm32_80.txt #
################################################
$

Using the Interactive Customization Menu (No Code)
This is the easiest method if you prefer a graphical menu interface.
Press M-x (usually Alt + x), type customize-variable,
"which does not work for me" and press Enter.
Type shift-select-mode and press Enter.
Click the [Toggle] button on the screen to switch between On (t) and Off (nil).
Click [Apply and Save] at the top of the buffer to store the setting permanently.

Method 2:
~/.emacs
(setq shift-select-mode t)
(setq shift-select-mode nil)


Method 3:
For the current session
1: Press M-x and type set-variable, then press Enter.
2: Type shift-select-mode and press Enter.
3: Type nil or t and press Enter.

gThe IceBreaker.
I'm telling you I'm the shit. 👍

Registers
Sto ->
	Mv to Method 1:
	C-x r SPC 1		# sto in r(1)

	Mv to Method 2:
	C-x r SPC 2		# sto in r(2)

	Mv to Method 3:
	C-x r SPC 3		# sto in r(3)

Recall ->
	C-x r j 1
	C-x r j 2
	C-x r j 3

BOOKMARKS :
	C-x r b		# jump to bm
	C-x r m 	# make bm
	C-x r l		# list bm

	M-x bo-delete

But where ?
In ~/.emacs.d/bookmarks
bookmrk-use-annotations t, the annotations  are shown in sep window.
bookmark-default-file

12345678901234567890123456789012345678901234567890

VIEW-MODE :
M-x view-b <TAB> <Enter>
e = exit q = quit, but sucks really hard

When do the Python
M-x font-lock-mode Highlighting off
M-x font-lock-mode Highlighting on

font-lock-maximum-decoration [1-3]
M-x customize-themes

M-x (global-display-line-numbers-mode 1) # 1=on no toggle side effect
Turn it off fast M-x d-nu
M-x (column-number-mode 1) # 1=on no toggle side effect

 == U+0098
M-x set-variable <Enter> cursor-type <Enter> hollow <Enter>
(set-variable 'cursor-type '(hbar . 10))
M-: => (eval-expression) 
In ~/.emacs
   (setq-default cursor-type '(hbar . 10))

display-line-numbers-mode 
in ~/.emacs
   ( global-display-line-numbers-mode 1 )  # no toggle side effects
   (column-number-mode 1 )  # no toggle side effects

M-x package-install : magit


1: C-x g => Git repository: ~/BV/Emacs
2: <Enter>
3: Create repository in /home/tito/BV/Emacs/? [Y/N]

Untracked files (3) <TAB>
Press (s) to stage then (n) to go to nxt file
Press (s) to stage then (n) to go to nxt file
Press (s) to stage then (n) to go to nxt file

Press (c) to commit, press (c) again.
Commit comment, then C-c C-c

Head: main 01
> recent commits> <TAB>
  press (n) <TAB> 0d38ae0

q = Quit !! Buffer is gone
It's done

Making a remote Repo in terminal
$ gh repo create Emacs --source=. --remote=origin --public
$

C-x g M a
remote_name: Emacs
remote_URL: git@github.com:Etch4

C-P p => push 

C-r (isearch-backward)
C-r Ice
if C-a 
   Then cursor is on the (T)he Icebreaker.
if <Enter>
   Then cursor is on The Ic(e)breaker.

#############################################33
ISEARCH :
C-s C-r incremental

Esc $ => ispell
Alt-Shift $ => ispell

Search for ThumbsUp
C-s C-x 8 <Enter> 1f44d

C-x 8 e l emoji list
C-x 8 e d emoji description
SEARCH :
C-s <Enter> [string] <Enter>
C-r <Enter> [string] <Enter>

Alt-s w  # word isearch
M-s w 	 # word isearch
Alt-s w <Enter>		# word search
M-s w <Enter> 		# word search

Alt-s w C-r <Enter> # word search reversed
M-s w C-r <Enter>	# word search reversed

M-x dictionary-search <Enter> : [word-to-look-up]
M-x dictionary-tooltip-mode

Esc-$ => ispell
Alt-Shift-$ => ispell

REGEX SEARCh
C-M-s (isearch-forward-regexp)
C-M-r (isearch-backward-regexp)

REGEX
$.[*+?]\^
? => 0|1
* >= 0
+ > 0
[charset] [^charset]
[[:charclass:]]

U+00AA = ª
U+24D0 = ⓐ
U+FB00 = ﬀ

foo\|bar
\(. . .\)
ba\(na\)+  =>  banana
x\{4\} xxxx
x\{4,\} xxxx =>
x\{2,4\} xx xxx xxxx
M-% Alt-%
C-x Esc Esc repeat
q = quit
. = once
! = all
[Uu] = undo
C-r = recursive edit
C-w = Del
	edit new
		 C-Alt-c
e => minibuffer <Return>
E => Case
C-h : ? : F1 = help

C-/ undo
M-x revert-buffer
M-x se-va

TRANSPOSING :
C-t => 2 letters
M-t => 2 words
C-M-t 2 balanced expressions
C-x C-t 2 lines
M-x transpose-regions (2)

MACRO :
KBD Macro Ring C-x C-k prefix
C-x C-k C-k exe
C-x C-k C-p prev
C-x C-k C-n nxt
C-x C-k C-i insert counter
C-x C-k n name it
C-x C-k b bind
C-x C-k C-e edit macro
C-x C-k e newname <Return>
C-x C-k l list

FILES :
C-x C-f => ( find-file )
C-x C-v => ( find-alternate-file )
C-x C-r => ( find-file-read-only )
C-x 4 f => ( find-file-other-window )
C-x 5 f => ( find-file-other-frame )
C-x C-q = make read-only
find-file-run-dired nil
Alt-Shift : create-lockfiles t

Moet er een verandering doorgevoerd zijn ?
Ja, dan wordt de time-stamp automatisch geupdated.

REVERT :
C-x x g => revert-buffer-quick

DIRECTORY :
C-x C-d => (list-directory)
C-u C-x C-d => Verbose
M-x make-directory <Return> dirname <Return>
M-x delete-directory <Return> dirname <Return>
M-x rename-file <Return> ... <Return> ... <Return>
M-x copy-directory => cp -r
M-x copy-file
M-x delete-file
C-x i => (insert-file)

BUFFERS :
C-x C-b => (list-buffers)
C-x b => (switch-to-buffer)
C-x -> left nxt buffer
C-x <- right prev buffer
C-x 4 b => (switch-to-buffer-other-window)
C-x 4 f => (swtich-to-file-other-window)
C-x 5 b => (switch=to-buffer-other-frame)
C-x 5 f => (find-file-other-frame)

M-g M-g => (goto-line)
C-u M-g M-g (goto-line-other-	buffer)

C-u C-x C-b [files only]
C-x C-b *Buffer List*
CRM Buf   Size Mode   File
. [current]
 % [Read-Only]
 * [Modified]
 
Each frame has it's own sest of windows.
C-x 2 => (split-window-below)
C-x 3 => I | I (spit-window-right)
M-x window-swap-states <Return>
C-M v => scroll-other-window-down
C-S-M v => scroll-other-window-up

C-x 4 b => Buf
C-x 4 f => file
C-x 4 d => directory
C-x 4 m => mail
C-x 4 r => ro
C-x 4 [41] => ?

C-x 0 => DEL window
C-x 1 => 1 window
C-x 4 0 => DEL window + Buf
C-x w <Return> buf <Return> => DEL window showing buf
C-x ^ => taller hor
C-x - => shorter hor
C-x { => shrink vert
c-x } => enlarge vert
C-x + => same size

Use the same window
display-buffer => in other window
display-buffer =? make other window

M-x cd <Return> edit <Return>
M-x cd <Return> edit <Return>

global-tab-line-mode
C-x <-
C-x ->

mouse-set-point
mouse-set-region
http://gnu.org

C-x 5 C-f		find-file-other-frame
C-x 5 C-o		display-buffer-other-frame
C-x 5 .			xref-find-definitions-other-frame
C-x 5 0			delete-frame
C-x 5 1			delete-other-frames
C-x 5 2			make-frame-command
C-x 5 5			other-frame-prefix
C-x 5 b			switch-to-buffer-other-frame
C-x 5 c			clone-frame
C-x 5 d			dired-other-frame
C-x 5 f			find-file-other-frame
C-x 5 m			compose-mail-other-frame
C-x 5 o			other-frame
C-x 5 p			project-other-frame-command
C-x 5 r			find-file-read-only-other-frame
C-x 5 u			undelete-frame

(add-to-list 'default-frame-alist '(width . 90))
(add-to-list 'default-frame-alist 'height . 40))
(add-to-list 'default-frame-alist '(font . "Monospace-10"))

~/.emacs
( global-tab-line-mode 1 )
( tab-bar-mode 1 )

C-x t C-f		find-file-other-tab
C-x t RET		tab-switch
C-x t C-r		find-file-read-only-other-tab
C-x t 0			tab-close
C-x t 1			tab-close-other
C-x t 2			tab-new
C-x t G			tab-group
C-x t M			tab-move-to
C-x t N			tab-new-to
C-x t O			tab-previous
C-x t b			switch-to-buffer-other-tab
C-x t d			dired-other-tab
C-x t f			find-file-other-tab
C-x t m			tab-move
C-x t n			tab-duplicate
C-x t o			tab-next
C-x t p			project-other-tab-command
C-x t r			tab-rename
C-x t t			other-tab-prefix
C-x t u			tab-undo


So Yesterday
 C-x 5 2 => create new frame
 C-x 5 o => other
 C-x 5 0 => DEL frame
 C-h h => hello thing
 C-h L
 C-x 8 <Return>
 C-\ => input method
 C-x <Return> c cp850 <Return> C-x C-f [file] <Return>

(MAJOR MODE)
	   text
	   programming
	   Dired, Mail

Major mode, then add function to that mode
(add-hook 'c++-mode-hook 'display-line-numbers-modae)
(add-hook 'c-mode-hook 'display-line-numbers-mode)

MINOR MODE
	  auto-fill-mode (toggle)
	  You can enable | disable it


Chap 21 Indentation
C-M-\ => (indent-region)
(tab-width_
(indent-tab-mode nil)

There are cmds to convert TABS and SPC
From SPC-to-TABS M-x tabify
From TABS-to-SPC M-x untabify

int a = 0;
short foo = 3

C-x h
M-x align

int		a	= 0;
short	foo = 3;

HN
text-mode
outline-mode
org-mode

C-x C-l => (downcase-region)
C-x C-u => (upcase-region)
M-l M-u M-c

Esc <TAB> completion-at-point

TEX :
dnf install texlive-scheme-basic
https://elpa.gnu.org/packages
M-x package-install <Return> auctex <Return>

Nroff Mode
M-x nroff-mode
The man pages

EDIT PROGRAMS :
     Indentation : Balanced Parentheses
     Alignment : Syntax Highlighting

Edit : Compile : Debug
M-x c-mode
	prog-mode-hook
	c-mode-hook

More Info => M-x info

Moving by Defun
ts => tree-sitter

C-M-a (beginning-of-defun)
C-M-e (end-of-defun)
C-M-h (mark-defun)

(which-function-modes t)
C-M-q
M-q
C-c C-q => (c-indent-defun)
C-c C-q => (c-ts-mode-indent-defun)

C-x h Esc C-\

M-x c-set-style <Return> ?
k&r, bsd, linux, python, gnu, stroustrup, java

(setq c-default-styl
	  '((java-mode . "java")
	  (c-mode . "k&r")
	  (other . "gnu")))


Balanced expressions
C-M-[f, b, k]

In C languages

M-;
/* works best in active region */

C-x C-; toggle comments
EL => Emacs Lisp

COMPILATION :
M-x compile
M-x recompile => also g in *compilation* buffer
M-: compile-command
U+2191
C-↑ = previous cmd in M-x shell
U+2193
C-↓ = next cmd in M-x shell

The *compilation* buffer uses major mode compilation-mode.
if (compile-error)
   then ( *compilation* has underlined line)
   goto-line <Return>
You are on the ERROR.

M-x se-va
compilation-auto-jump-to-first-error
t

M-x flymake-mode
Python debugger PDB
GNU debuggers
M-x gdb
M-x gud-gdb
M-x pdb

M-x se-va
gdb-many-windows
t

When done with debugging
kill the frame C-x k

if M-x ielm
   then ELISP>

Only for the first time.
*vc-log*
[C-x v v] ^2

C-x v v => Summary: 19 C-c C-c
This stages and commit.

C-x v P => (vc-push) =>  git push
Is this gonna work also.

SUMMARY :
C-x v v <Comment> C-c C-c
C-x v P

emacs ~/.config/git/ignore
\*vc-log\*

$ git ls-files

git restore --staged <file>
.gitignore in MAIN DIRECTORY, not .git
Also add .gitignore itself in it

C-x v i => track file, register file
C-x v l => (vc-print-log)

C-x v d (vc-dir)  == $ git status
VC backend : Git
Working dir: ~/BV/Emacs/
Branch     : main
Stash      : Nothing stashed

                         ./
     unregistered        doit
     edited              emacs_01.txt

while in VC-DIR Mode
C-x v G put file in .gitignore

M-x abbrev-mode

Dired
C-x C-d => (list-directory)
C-x d => (dired)
C-x d *.c <Return>
C-x 4 d => (dired-other-window)
C-x 5 d => (dired-other-frame)


offlineimap
notmuch new
M-x notmuch
C-x m = new mail
C-c C-f C-t => To: line
C-c C-f C-s => Subject: line
C-c C-a => attach file
C-c C-a => attach file
r is reply
then error => ignore => sending ... done.

M-x eshell is better

M-S-x % => query-replace
Esc S-% => query-replace
LVL_01
C-r => recursive edit
LVL_02
C-M-c
LVL_01

M-x print-buffer
M-x browse-url <Return> URL: <Return>

file tetris.el in /opt/Tetris
In .emacs
(add-to-list 'load-path "/Opt/Tetris")
(require 'tetris)

C-s incr-search <Return>
C-s C-s repeat
C-s C-r repeat

$ ln -s source link
GNU ELPA https://elpa.gnu.org
GNU ELPA https://elpa.nongnu.org

M-x package-upgrade-all

Emacs Key: /usr/share/emacs/30.2/etc/package-keyring.gpg
(setq package-check-signature nil)

The packages are in ~/.emacs.d/elpa
M-x package-list

M-x magit werkt nog als een nieuwe.
Doet Emacs VC het ook nog ??

C-h v (describe-variable)
M-x set-variable
M-: (eval-expression)

~/.local/share/Trash
C-h b current keybinding

Prefix KB C-x C-c

~/.emacs
keymap-global-set <Return> C-z shell <Return>
keymap-set <Return> C-z shell <Return>

The github
$ git --global user.name "..."
$ git --global user.email "..."

# dnf5 install ogg123 mpg123 aplay sox
sox include play

This is the shit !!!
Esc-! shell-cmd: play ~/Music/ni_final.wav &
Luisteren zonder emacs te verlaten 

